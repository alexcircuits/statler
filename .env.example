# GitHub Personal Access Token (optional, increases rate limit from 60 to 5000 req/hour)
# Create at: https://github.com/settings/tokens (no special scopes needed for public data)
GITHUB_TOKEN=

# Server port (default: 3000)
PORT=3000
